<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"
        tools:ignore="PackageInstallPermission" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- 长驻消息桥接通道（微信/QQ/保活）改用 specialUse：dataSync 类型在 Android 15 共享 6h/24h
         预算，耗尽后 FGS 全部被杀且后台无法重启（权限无法解决）；specialUse 无系统时限。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 电池优化白名单：ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS 必需，否则系统直接 SecurityException -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- IQOO/OriginOS JobScheduler 保活 -->
    <uses-permission android:name="android.permission.BIND_JOB_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <!-- 病娇模式：读取应用使用统计（需用户在系统设置中授权） -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- 病娇模式：Android 11+ 包可见性。未声明 <queries> 时 getInstalledApplications()
         只会返回本应用自身，导致已安装应用列表为空、病娇模式永远无法生成上下文提示。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- 瑞幸咖啡：定位附近门店 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- OPPO / vivo 厂商 Push 权限 -->
    <uses-permission android:name="com.coloros.mcs.permission.RECEIVE_MCS_MESSAGE" />
    <uses-permission android:name="com.heytap.mcs.permission.RECEIVE_MCS_MESSAGE" />
    <uses-permission android:name="com.vivo.push.permission.RECEIVE" />

    <permission
        android:name="${applicationId}.permission.PUSH"
        android:protectionLevel="signature" />

    <application
        android:name=".security.StaticApkShell"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YuNian"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- libvndksupport.so / libOpenCL.so 的 uses-native-library 声明已随 LiteRT-LM 删除：
             二者仅被 libLiteRt.so / libLiteRtClGlAccelerator.so / liblitertlm_jni.so 所需，
             而这三个 .so 已随 feature:localmodel 退役（D4）。liblianyu_agent.so 不依赖它们。 -->

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.yunian.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:ignore="TypoInNamespace">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name="com.yunian.ai.security.SService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="虚拟恋人后台守护与定时消息调度" />
        </service>

        <service
            android:name="com.yunian.ai.feature.qqbot.service.QQBotForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="QQ 机器人长连接消息桥接" />
        </service>

        <service
            android:name="com.yunian.ai.feature.notification.CompanionKeepAliveService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="虚拟恋人后台守护与定时消息调度" />
        </service>

        <!-- IQOO/OriginOS 第三层兜底保活 JobService -->
        <service
            android:name=".IqooKeepAliveJobService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <receiver
            android:name="com.yunian.ai.KeepAliveAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name="com.yunian.ai.feature.notification.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.yunian.ai.security.SReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".security.SActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.YuNian"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.YuNian"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize" />

        <!-- vivo Push 密钥配置（未配置时留空，SDK 会跳过注册） -->
        <meta-data
            android:name="com.vivo.push.api_key"
            android:value="${VIVO_PUSH_API_KEY}" />
        <meta-data
            android:name="com.vivo.push.app_id"
            android:value="${VIVO_PUSH_APP_ID}" />

        <!-- OPPO Push 服务（兼容 ColorOS 各版本 action） -->
        <service
            android:name=".push.service.OppoPushService"
            android:exported="true"
            android:permission="com.heytap.mcs.permission.SEND_PUSH_MESSAGE">
            <intent-filter>
                <action android:name="com.heytap.mcs.action.RECEIVE_MCS_MESSAGE" />
                <action android:name="com.heytap.msp.push.RECEIVE_MCS_MESSAGE" />
            </intent-filter>
        </service>
        <service
            android:name=".push.service.OppoPushServiceLegacy"
            android:exported="true"
            android:permission="com.coloros.mcs.permission.SEND_MCS_MESSAGE">
            <intent-filter>
                <action android:name="com.coloros.mcs.action.RECEIVE_MCS_MESSAGE" />
            </intent-filter>
        </service>

        <!-- vivo Push 服务与接收器 -->
        <service
            android:name="com.vivo.push.sdk.service.CommandClientService"
            android:exported="true"
            android:permission="com.vivo.push.permission.RECEIVE"
            tools:replace="android:permission" />
        <activity
            android:name="com.vivo.push.sdk.LinkProxyClientActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <receiver
            android:name=".push.receiver.VivoPushReceiver"
            android:exported="true"
            android:permission="com.vivo.push.permission.RECEIVE">
            <intent-filter>
                <action android:name="com.vivo.pushclient.action.RECEIVE" />
            </intent-filter>
        </receiver>

        <!--
        小米 Push 接收器
        引入 MiPush SDK aar 后取消注释，并创建 XiaomiPushReceiver 继承 PushMessageReceiver。
        <receiver
            android:name=".push.receiver.XiaomiPushReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.xiaomi.mipush.RECEIVE_MESSAGE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.MESSAGE_ARRIVED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.ERROR" />
            </intent-filter>
        </receiver>
        -->

        <!-- 华为 HMS Push 服务 -->
        <service
            android:name=".push.service.HuaweiPushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.huawei.push.action.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
